# Session (required in production)
SESSION_SECRET=

# Base URL for links in emails (e.g. https://your-app.example.com)
ORIGIN=http://localhost:5173

# Optional: SQLite path (default: ./data/reservation.db)
# DATABASE_PATH=./data/reservation.db

# --- Proton Mail direct SMTP (paid plan with custom domain) ---
# Create an SMTP token: Settings → Proton Mail → IMAP/SMTP → SMTP tokens.
# Use the token as SMTP_PASS (not your account password).
SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@yourdomain.com
SMTP_PASS=your-smtp-token
# Optional: from address (defaults to SMTP_USER)
# SMTP_FROM=
# Optional: from display name
# SMTP_FROM_NAME=Terrible Football Liverpool
